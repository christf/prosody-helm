apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prosody-helm.fullname" . }}-test-connection"
  labels:
    {{- include "prosody-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prosody-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
